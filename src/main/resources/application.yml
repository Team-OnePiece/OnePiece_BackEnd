spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${url}
    username: ${username}
    password: ${password}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      show_sql: true

  jwt:
    key: ${JWT_KEY}
    live:
      accessToken: ${ACCESSTOKEN_TIME}
