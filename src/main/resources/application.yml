spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${URL}
    username: ${USERNAME}
    password: ${PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      show_sql: true

  jwt:
    key: ${JWT_KEY}
    live:
      accessToken: ${ACCESS_TOKEN_TIME}
